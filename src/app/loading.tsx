"use client";

import React from "react";
import { Loading } from "@/components/ui";

export default function RootLoading() {
  return <Loading variant="fullscreen" message="Carregando o Compra+..." />;
}
